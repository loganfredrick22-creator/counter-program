# counter-program
A simple interactive counter web app built with HTML, CSS, and JavaScript. Users can increase, decrease, or reset a number using buttons, while the display updates in real time using DOM manipulation.
